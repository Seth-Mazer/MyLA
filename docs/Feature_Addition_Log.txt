***FEATURE ADDITION LOG****
**THIS IS PURELY A FEATURE ADDITION LOG, BUG FIXES / MINOR LOGIC UPDATES FOR EXAMPLE WOULD NOT BE INCLUDED**

> 6/15/26 Added det(), returns the determinant of a matrix, also re-wrote solve(), and added solve_e(). solve() now copies A to B, and calls solve_e() with B. Thus leaving A intact.

> 6/11/26 Added solve(), calling solve is essentially a wrapper for calling LU(), backSub(), and forSub(). Its contained nice and neat, so
all you need is to define your matrix A, and your vector B, and call solve(A,B). You don't need to directly use LU/forSub/backSub. However, they are available,
if you have triangular systems.

> 6/10/2026 Finally added LU Factorization, using a partial pivoting and outer product (trailing block) computation method. Calling LU(A)
 will return the packed factorization of LU.

> 6/9/2026 > Added a outerProduct function. Which takes two matrices in n x 1 and 1 x n form, and returns the outer product

> 6/4/2026 > Added A.normalize() which mutates A, and added A.normalized() which returns a normalized copy

> 6/1/2026 > Added elementary row operations

> 5/31/2026 > Added augment function. Can now create [ A | B ].
Added A.getCol(i) and A.getRow(i), to retrieve respective columns or rows of matrices

> 5/30/2026 > Added print function. Now you can print your matrices to the console

> 5/30/2026 > Added == operator, to check if matrices are equal, added isSymmetric to check if matrices are symmetric
added transpose as a member function as well


> 5/30/2026 > Added A.identity(), member function. Returns identity matrix of a square matrix